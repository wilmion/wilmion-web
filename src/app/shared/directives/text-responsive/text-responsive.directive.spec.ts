import { TextResponsiveDirective } from './text-responsive.directive';

describe('TextResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});

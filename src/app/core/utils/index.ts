import { petition } from './api';
import { elegibleDate } from './date';
import { chunkArray, createArrayToElement } from './extendArray';
import { getValue } from './forms';
import { getIconBackendOrFrontend } from './Icons';
import { getImageFromFile, getFileFromUrl } from './Image';
import {
  VerbsButton,
  getVerbsFromButton,
  createList,
  getLastDate,
  getPaths,
} from './Others';
import {
  decodeText,
  addText,
  addTextInPart,
  editText,
  removeText,
} from './language_text';

export {
  petition,
  elegibleDate,
  chunkArray,
  createArrayToElement,
  getValue,
  getIconBackendOrFrontend,
  getImageFromFile,
  getFileFromUrl,
  VerbsButton,
  getVerbsFromButton,
  createList,
  getLastDate,
  getPaths,
  decodeText,
  addText,
  addTextInPart,
  editText,
  removeText,
};
